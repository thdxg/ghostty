name: Assert No Drift

# Positive guard that this fork carries ONLY its declared downstream delta on
# top of pristine upstream — the invariant sync-upstream.yml maintains
# (macterm#168). The old sync bug silently reverted ~190 upstream files while
# the parent pointer looked current, shipping a stale libghostty that resurfaced
# macterm#112. sync-upstream was rewritten to be revert-proof; this workflow is
# the assertion that nothing has drifted anyway, so a regression is caught here
# instead of by a user reporting a downstream symptom months later.
#
# It works off the MERGE-BASE of main with upstream/main, NOT HEAD~1. The daily
# sync collapses the delta to a single commit, but between a PR merge and the
# next sync `main` legitimately carries extra commits (merge commit + PR
# commits) that the next sync will squash away — anchoring on HEAD~1 would then
# false-alarm. The merge-base is stable across both shapes.
#
# Fails loudly when either:
#   1. the merge-base is too far behind upstream/main HEAD (MAX_AGE_DAYS) — the
#      scheduled sync has stopped, so the artifact is going stale; or
#   2. the set of files main changes relative to that merge-base includes any
#      path outside the allowlist (DOWNSTREAM_FILES + the paths each
#      .github/downstream/*.patch declares) — i.e. it reverted or edited
#      upstream source it shouldn't. This file-set check is the real guarantee;
#      it holds regardless of how the downstream commits are structured.
#
# A second job, `reproducible`, runs on every pull request to main and every
# push to it. It replays the sync (pristine upstream + DOWNSTREAM_FILES +
# .github/downstream/*.patch) onto the tree's upstream base and fails unless
# the result IS that tree. The file-set check above can't see a PR that edits
# src/ without regenerating the patch that carries it — the path is
# allowlisted — and the next sync, which rebuilds main from the patches alone,
# then drops the change without a word: thdxg/ghostty#11 was lost that way on
# 2026-09-23.

on:
  schedule:
    - cron: "0 7 * * *" # an hour after sync-upstream's 6am run
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

env:
  # Must match DOWNSTREAM_FILES in sync-upstream.yml. Kept as a literal (not
  # sourced from that file) so a corruption of sync-upstream.yml itself can't
  # widen the allowlist unnoticed.
  DOWNSTREAM_FILES: >-
    .github/workflows/build-ghosttykit.yml
    .github/workflows/sync-upstream.yml
    .github/workflows/assert-no-drift.yml
    .github/downstream/0001-command-wrapper.patch
    .github/downstream/0002-output-activity.patch
    .github/downstream/0003-sync-environ.patch
    .github/downstream/0004-background-default-transparent.patch
    .github/downstream/0005-smooth-scroll.patch
    .github/downstream/0006-custom-shader-cursor-hidden-offscreen.patch
  # How far behind upstream HEAD the fork's base may fall before the sync is
  # presumed broken. A daily sync should keep this at 0–1 days.
  MAX_AGE_DAYS: "4"

jobs:
  assert:
    # The freshness half would fail an unrelated PR whenever the sync is
    # behind, so this job keeps to its schedule.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Assert only the allowlisted downstream delta rides on pristine upstream
        run: |
          set -euo pipefail
          git remote add upstream https://github.com/ghostty-org/ghostty.git
          git fetch --quiet upstream main

          upstream_head="$(git rev-parse upstream/main)"
          # The fork's upstream base = where main last diverged from upstream.
          # Stable whether the downstream delta is one squash commit or a
          # merge + PR commits awaiting the next sync.
          base="$(git merge-base HEAD upstream/main)"
          echo "Upstream base (merge-base) = $base"
          echo "Upstream HEAD             = $upstream_head"

          # (1) Freshness: how far behind upstream HEAD is that base?
          behind="$(git rev-list --count "${base}..${upstream_head}")"
          echo "Fork base is $behind commit(s) behind upstream/main."
          if [ "$base" = "$upstream_head" ]; then
            echo "Base is exactly upstream/main (a fresh sync)."
          else
            age_days="$(( ( $(git log -1 --format=%ct "$upstream_head") - $(git log -1 --format=%ct "$base") ) / 86400 ))"
            echo "Base commit is ~${age_days} day(s) older than upstream HEAD."
            if [ "$age_days" -gt "$MAX_AGE_DAYS" ]; then
              echo "::error::Fork base is ~${age_days} days behind upstream (> ${MAX_AGE_DAYS}). The scheduled sync has likely stopped."
              exit 1
            fi
          fi

          # (2) Every declared downstream file must exist. A missing patch must
          # fail loudly rather than silently dropping its source changes on the
          # next reset to upstream.
          for f in $DOWNSTREAM_FILES; do
            if [ ! -f "$f" ]; then
              echo "::error::Declared downstream file is missing: $f" >&2
              exit 1
            fi
          done

          # …and the converse: every patch on disk must be declared. Only the
          # declared list is copied across the sync's reset to upstream, so an
          # undeclared patch is a change that will vanish. Without this the
          # symptom surfaces below as the patch file itself showing up as an
          # unallowlisted path, which reads as "main is reverting upstream
          # source" — the wrong diagnosis for a one-line omission here.
          # Normalized to single-space separators so the membership test below
          # doesn't depend on how the YAML scalar folds its whitespace.
          declared=" "
          for f in $DOWNSTREAM_FILES; do declared="${declared}${f} "; done
          shopt -s nullglob
          for p in .github/downstream/*.patch; do
            case "$declared" in
              *" $p "*) ;;
              *)
                echo "::error::Downstream patch is not declared in DOWNSTREAM_FILES: $p" >&2
                echo "::error::Add it to DOWNSTREAM_FILES in BOTH assert-no-drift.yml and sync-upstream.yml." >&2
                exit 1
                ;;
            esac
          done

          # (3) Build the allowlist: DOWNSTREAM_FILES + every path declared
          # inside each patch (its `+++ b/<path>` lines), so adding a new .patch
          # self-updates the allowlist without editing this workflow.
          allow="$(mktemp)"
          for f in $DOWNSTREAM_FILES; do echo "$f"; done >> "$allow"
          shopt -s nullglob
          for p in .github/downstream/*.patch; do
            grep -E '^\+\+\+ b/' "$p" | sed -E 's#^\+\+\+ b/##' >> "$allow"
          done
          sort -u "$allow" -o "$allow"
          echo "Allowlisted downstream paths:"; sed 's/^/  /' "$allow"

          # Every file main changes vs the upstream base must be allowlisted.
          # This is the drift guarantee: reverting/editing upstream source would
          # show a path here that isn't declared.
          changed="$(git diff --name-only "$base" HEAD)"
          echo "Files changed vs upstream base:"; echo "$changed" | sed 's/^/  /'
          unexpected=""
          while IFS= read -r path; do
            [ -z "$path" ] && continue
            grep -qxF "$path" "$allow" || unexpected="${unexpected}${path}"$'\n'
          done <<< "$changed"
          if [ -n "$unexpected" ]; then
            echo "::error::main diverges from upstream in paths outside the allowlist — it may be reverting or editing upstream source:"
            echo "$unexpected" | sed 's/^/  /'
            echo "::error::If this is an intentional new downstream change, add it as a .github/downstream/*.patch (or to DOWNSTREAM_FILES) so it's declared."
            exit 1
          fi

          echo "OK: main rides only the allowlisted downstream delta on pristine upstream."

  reproducible:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Assert the sync reproduces this tree
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/ghostty-org/ghostty.git
          git fetch --quiet upstream main

          head="$(git rev-parse HEAD)"
          base="$(git merge-base HEAD upstream/main)"
          echo "Tree under test          = $head"
          echo "Upstream base (merge-base) = $base"

          # The files the sync lays down verbatim: sync-upstream.yml's own
          # DOWNSTREAM_FILES, read from this tree, since that is the list the sync
          # will run with once this lands.
          files="$(awk '/^  DOWNSTREAM_FILES: >-/{f=1;next} f&&/^    /{print $1;next} f{exit}' .github/workflows/sync-upstream.yml)"
          if [ -z "$files" ]; then
            echo "::error::Could not read DOWNSTREAM_FILES from sync-upstream.yml." >&2
            exit 1
          fi
          # ...and it must be this workflow's list too, which the scheduled job
          # checks the patches against. Both are edited by hand.
          if [ "$(printf '%s\n' $files | sort)" != "$(printf '%s\n' $DOWNSTREAM_FILES | sort)" ]; then
            echo "::error::DOWNSTREAM_FILES differs between sync-upstream.yml and assert-no-drift.yml; update both." >&2
            exit 1
          fi

          # Replay the sync: pristine upstream, the fork-owned files on top, then
          # every source patch in filename order, exactly as sync-upstream.yml does.
          replay="$(mktemp -d)"
          git worktree add --quiet --detach "$replay" "$base"
          for f in $files; do
            if [ ! -f "$f" ]; then
              echo "::error::Declared downstream file is missing: $f" >&2
              exit 1
            fi
            mkdir -p "$replay/$(dirname "$f")"
            cp "$f" "$replay/$f"
            git -C "$replay" add "$f"
          done
          shopt -s nullglob
          for p in "$replay"/.github/downstream/*.patch; do
            echo "Applying downstream patch: ${p#"$replay/"}"
            if ! git -C "$replay" apply --3way --index "$p"; then
              echo "::error::Downstream patch does not apply to upstream: ${p#"$replay/"}" >&2
              exit 1
            fi
          done

          # What the sync would produce must be this tree, file for file. A path
          # that differs is a change the sync would drop or rewrite.
          if ! git -C "$replay" diff --cached --quiet "$head"; then
            echo "::error::The next sync would not reproduce this tree. Paths that differ between it and pristine upstream + DOWNSTREAM_FILES + .github/downstream/*.patch:" >&2
            git -C "$replay" diff --cached --stat "$head" | sed 's/^/  /' >&2
            echo "::error::A source change has to reach main as a regenerated .github/downstream/*.patch in the same PR; the sync rebuilds main from the patches alone and drops anything else (thdxg/ghostty#11 was lost this way on 2026-09-23)." >&2
            exit 1
          fi
          echo "OK: the sync reproduces this tree exactly."
